const teacher = "30";
// if (teacher > 30) {
///// output teacher is not come in the class
if (teacher == 30) {
  // output teacher in a class
  console.log("teacher in a class");
} else {
  console.log("teacher is not come in the class");
}

const score = 100;
if (score > 90) {
  const power = "fly";
  console.log(`user power ${power}`);
}
